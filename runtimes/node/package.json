{
  "name": "fa-runtime-node",
  "version": "0.14.5",
  "description": "Facet Architecture server runtime for Node.js — interprets the neutral render IR (render.json) emitted by `fct build` with target = \"node\". Zero dependencies.",
  "license": "Apache-2.0",
  "main": "facet.js",
  "scripts": {
    "demo": "node demo/server.js"
  },
  "engines": { "node": ">=18" }
}
