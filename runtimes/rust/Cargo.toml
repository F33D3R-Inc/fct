[package]
name = "fa-runtime"
version = "0.14.5"
edition = "2021"
description = "Facet Architecture server runtime for Rust — interprets the neutral render IR (render.json) emitted by `fct build` with target = \"rust\". Dependency-free (std only)."
license = "Apache-2.0"

[lib]
name = "fa_runtime"
path = "src/lib.rs"

[[bin]]
name = "demo"
path = "src/bin/demo.rs"

[dependencies]
