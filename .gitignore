# build artifacts (release binaries are published as GitHub Release assets)
/facet
/fbin
/dist/

# scaffolded test projects
/demoapp

# legacy local app data (pre-Postgres)
*.data.json
examples/*.data.json
