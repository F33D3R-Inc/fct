# Home — a wireframe-level facet that mounts on the Playground (fa:root).
# It's a plain facet: edit the looks block and refresh.

facet Home:
    looks:
        <section class="fa-home">
            <h1>It works.</h1>
            <p>This page is a Facet Architecture <b>Playground</b>. Facets live in
               <code>facets/</code> — server-rendered, pushed live over SSE.</p>
        </section>
