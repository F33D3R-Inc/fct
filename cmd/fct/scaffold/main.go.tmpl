// __MODULE__ — a Facet Architecture app.
//
// This file is the entrypoint and nothing else. It is intentionally inert: it
// hands off to the app package and returns. You should never add features here.
//
// Your app lives in two places:
//   - facets/   — your UI, written in the .fct language
//   - app/      — one Go file per feature (routes, events, server-side state)
//
// To add a feature, add a file under app/ — never here.
package main

import "__MODULE__/app"

func main() {
	app.Main()
}
