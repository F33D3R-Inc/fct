# Avatar — a rounded avatar image, deterministically seeded from a name/handle.
# Pure presentation: `use Avatar(t.author)`.
app FacetUI:
    component Avatar(name: text):
        image "https://api.dicebear.com/7.x/thumbs/svg?seed={name}"
