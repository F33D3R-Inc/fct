# VerifiedBadge — the blue check beside a verified name. Shown only when
# `verified` is true; the glyph comes from icons.fct.
import "icons.fct"
app FacetUI:
    component VerifiedBadge(verified: bool):
        if verified:
            icon "verified"
