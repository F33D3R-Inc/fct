# Trend — one row in the "What's happening" rail.
app FacetUI:
    component Trend(category: text, tag: text, count: text):
        box:
            text "{category} · Trending"
            text "{tag}"
            text "{count}"
