# Nav — the left rail. A `ui` facet: pure presentation that snaps into the `nav`
# socket of a wireframe. No data, no durable state.
ui Nav in nav:
    content:
        text "𝕏 · F33D3R"
        link "Home" -> "/"
        text "signed in as {actor}"
        button "log out" -> logout
