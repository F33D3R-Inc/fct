# UnreadBadge — a count pill that only shows when there is something unread. Pass a
# count from the host (e.g. count(n in Notification where n.user == actor && !n.read)).
app FacetNotify:
    component UnreadBadge(n: int):
        if n > 0:
            badge "{n}"
