# SearchBox — the explore/search input. Binds the host's @client `q` cell; the host
# filters its feed with `where contains(lower(field), lower(q))`. Pure presentation.
app FacetForms:
    component SearchBox():
        box:
            input bind q placeholder "Search"
