# detach outside a daemon body is refused: a proc's concurrency is structured.
app DetachInProc:
    proc work(n: int) -> int:
        return n

    proc outer():
        detach work(1)

    action go:
        do outer()
