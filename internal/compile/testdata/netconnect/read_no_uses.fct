app A:
    state result: int = 0
    proc peek(c: int) -> int:
        return len(readBytes(c, 1))
    action run(c: int):
        let r = do peek(c)
        result = r
    view Home at "/":
        box:
            text "{result}"
