app A:
    state result: int = 0
    proc dial(host: text) -> int uses io.net:
        return connect(host)
    action run(host: text):
        let r = do dial(host)
        result = r
    view Home at "/":
        box:
            text "{result}"
