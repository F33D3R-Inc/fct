app A:
    state result: int = 0
    proc serve(port: int) -> int uses io.net.listen:
        return listen(port)
    action run(port: int):
        let r = do serve(port)
        result = r
    view Home at "/":
        box:
            text "{result}"
