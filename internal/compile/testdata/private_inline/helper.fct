app PrivateInlineHelper:
    # A file-local helper called inline, in expressions, not only via `do`.
    private proc twice(n: int) -> int:
        return n * 2

    proc helperResult(n: int) -> int:
        let doubled = twice(n) + 1
        if twice(doubled) > 10:
            return twice(doubled)
        return doubled
