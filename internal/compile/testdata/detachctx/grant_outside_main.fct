app GrantOutsideMain:
    # grantRead is main's alone.
    proc load(path: text) -> text uses io.file:
        let ok = grantRead(path)
        return readFile(path)

    proc main(args: [text]) -> int uses io.file:
        let t = do load(args[0])
        return 0
