import "chat.fct"

app Routes:
    type PingDTO:
        at: text
    api GET "/api/v2/ping" -> ping
    action ping() -> PingDTO:
        return PingDTO{at: iso(now())}
    view Home at "/":
        text "x"
