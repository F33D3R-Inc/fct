# detach from an ordinary proc: both compilers refuse it.
app DetachInProcParity:
    proc work(n: int) -> int:
        return n

    proc outer():
        detach work(1)

    action go:
        do outer()
