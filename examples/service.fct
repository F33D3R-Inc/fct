app SvcDemo:
    entity Note:
        id: int
        body: text
    service Moderation at "http://localhost:9099":
        review(body: text)
    action post(body: text):
        add Note { body: body }
        call Moderation.review(body)
    view Home at "/":
        box:
            text "service demo"
