# The login screen's wireframe — a single centered socket for the sign-in form.
# A different wireframe than the home Shell, mounted at a different route; that is
# what makes login a separate surface from the feed.
import "loginform.fct"

wireframe Auth:
    socket form: ui

    frame:
        box:
            slot form
