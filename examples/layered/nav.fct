# The left rail — a ui facet in the `nav` socket. This is the home screen, which
# is member-only, so the nav can assume a signed-in actor.
ui Nav in nav:
    content:
        text "𝕏 · F33D3R"
        link "Home" -> "/"
        text "signed in as {actor}"
        button "log out" -> logout
